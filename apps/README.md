# /apps — Deployable applications

Planned (see docs/roadmap/):

- `api` — the Chronos modular-monolith API (Phase 9)
- `web` — the reference web client (Phase 11)
- `workers` — event-bus consumers / background jobs (Phase 8)
- `tiles` — tile-server configuration & deployment (Phase 17)

Empty until those phases. Nothing lands here without its roadmap phase.
