# /data — Small fixture & reference data ONLY

Golden test vectors, known-world fixtures, seeded review suites. Large datasets
live in object storage and are pulled by pipelines (docs/04 §3); raw dataset
drops never enter git.
