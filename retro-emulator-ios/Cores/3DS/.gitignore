# Vendored snapshots must not lose files to ancestor ignore rules
# (root dist/ and build/ patterns). Negations re-include the dirs
# then their contents (order matters for git).
!azahar/dist/
!azahar/dist/**
!azahar/externals/zstd/build/
!azahar/externals/zstd/build/**
